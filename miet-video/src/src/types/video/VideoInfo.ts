export interface VideoInfo {
    id: string,
    source: string,
    preview?: string,
    resolution: number,
    type: string,
    title: string,
    author: string,
    description?: string,
    date: string,
}