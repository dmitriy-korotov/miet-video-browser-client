export interface VideoInfo {
    id: string,
    source: string,
    preview?: string,
    resolution: number,
    video_type: string,
    preview_type?: string,
    title: string,
    author: string,
    description?: string,
    date: string,
}